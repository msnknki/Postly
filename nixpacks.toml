# Railway/Nixpacks configuration for Postly Backend
# This tells Railway to build from the server directory

[phases.setup]
nixPkgs = ["nodejs-18_x", "npm"]

[phases.install]
cmds = ["cd server && npm ci"]

[start]
cmd = "cd server && npm start"

